# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Temp files
*.tmp
~$*

# OS
.DS_Store
Thumbs.db

# EDINET downloads (large XBRL, re-fetchable via API)
edinet_data/
tmp/

# Editor
.vscode/
*.swp

# Environment
.env
.venv/
venv/

# Personal files (never commit by accident)
*.pdf
*.lnk
# Published research reports are the exception to the PDF rule
!reports/*.pdf

# Excel lock/owner files
~$*.xlsx
~$*.xls

# Generated models (regenerable via generate_dcf.py; may sit next to personal files)
models/*
# ...except the regression baselines: *_regen.xlsx is the output of re-generating
# a reference model with the current template, and the whole point of keeping it
# is that scripts/diff_models.py can be re-run against it after a template change.
!models/*_regen.xlsx
!models/*_regen_validation.txt


# Screener archives and DB (large, and TDnet raw is re-fetchable only for ~1 month
# — the archive is a local asset, not a repo artefact). Config/schema/code ARE tracked.
screener/data/raw/
screener/data/cache/
screener/data/logs/
screener/data/*.db
screener/data/*.db-wal
screener/data/*.db-shm
