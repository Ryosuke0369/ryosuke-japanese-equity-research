"""screener/technical — テクニカル層（シャドウE・2026-09-19 事前登録）。

設計原則（docs/backtest_acceptance_criteria.md E-0。投資方針に由来し、変更しない）
--------------------------------------------------------------------------------
テクニカルは**証拠で選ばれた銘柄に対してのみ作用する**。できるのは
拒否（veto）・タイミング調整・撤退判定の3つだけで、**候補の追加とスコアへの
加点はしない**（証拠 → 織り込み → 需給の順序。「需給を入口の根拠にしない」）。

これを約束ではなく構造で守るために:

- スコア・候補を作る側（signals / projection / extract / weekly_screen /
  paper_weekly / backtest_v2 / backtest_eval）はこのパッケージを import しない
- このパッケージはスコア・採否・建玉のテーブルに書き込まない
- このパッケージは signals の採点関数を呼ばない

いずれも `screener/tests/test_technical_isolation.py` が検査する。

現在の中身はフェーズA（イベント→価格の反応の**測定**）だけで、シグナルは無い。
"""
