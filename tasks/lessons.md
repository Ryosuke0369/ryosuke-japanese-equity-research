# Lessons

## 位置ベースの配列結合は「静かな年度ズレ」を生む (2026-07-31)
**Mistake (構造):** EDINET 由来の OCF/現金/有利子負債を `hist_years` に**位置で**
コピーしていた。overrides が `hist_years` を丸ごと差し替える（年数もラベルも違う）
運用と組み合わさり、5銘柄すべてで**最大3年ぶんズレた値**が別年度の見出しの下に
座っていた（3687: FY2025/9 欄の OCF 1,488 は実は FY2022/9 の値）。値は実在し、
桁も自然なので**目視では絶対に気づかない**。

**Rule for myself:**
- 2つの系列を結合するときは**必ずキーで突合**する。`zip()`／インデックス一致は
  「両者が同じ順序・同じ長さである」という検証されていない仮定であり、その仮定が
  崩れた瞬間に沈黙して壊れる。
- 突合できなかった要素は**空欄にして警告**する。詰める・ずらす・0で埋めるは禁止
  （「データが無い」と「値が0」は別物）。
- 突合ラダー（完全一致→年+月→年が両側一意）を作ったら、**曖昧な場合は必ず空欄**に
  倒す。1件でも取りこぼしたくない気持ちが誤配を生む。
- 配列長は契約にしてバリデータで**実行前にエラー停止**させる。

## 生成物には「機械検証できる根拠」を同梱する (2026-07-31)
**Mistake (構造):** 「D27 が定数 1 になっている」「Comps 統計に自社が入っている」
「C5 が予測からの逆算」——いずれも**生成後に人が見て気づくしかない**壊れ方だった。
実際に 5銘柄中 3銘柄で数か月間そのまま運用され、生成後パッチスクリプト
（add_bank_valuation.py 等）で個別に手当てされていた。

**Rule for myself:**
- テンプレートを直したら、**同じ不具合を毎回検出する検査**（`validate_output.py`）を
  必ずセットで作る。修正だけでは再発を止められない（次の手修正で戻る）。
- 検査が「生成時の意図」と照合できるよう、**成果物自身に機械可読なメタデータ**を
  埋める（Adjustments Log の Pipeline Metadata）。数値から意図を逆算させない。
- 生成後パッチで直している症状があれば、それは**テンプレート側のバグの証拠**。
  パッチを増やさずテンプレートを直す。
- コード変更の影響は **main のワークツリーで同一入力を流した A/B 差分**で示す。
  「旧成果物との差分」は入力差（overrides の編集、株価、四半期進行）が混ざるため
  そのままでは証拠にならない。

## ライブ取得依存は再現性を壊す (2026-06-12)
**Mistake (構造):** comps の時価総額を生成時に yfinance ライブ取得していたため、
同一CSV・同一コードでも実行時刻によって implied 値が±数円変動した（5246 EV/Sales
455→456 等）。「同じ入力 → 同じ出力」が成り立たないと、回帰確認・差分レビュー・
過去成果物の検証がすべて「ドリフトか実変化か」の切り分けから始まることになる。
さらに行単位の「列に値があれば使う、なければライブ」というサイレント混在許容は、
1つの comps セット内で静的値とライブ値が黙って混ざる地雷だった。

**Rule for myself:**
- 最終成果物の入力は**全て静的ファイルに固定**する（comps CSV の Market_Cap 列、
  overrides の current_price）。ライブ取得は「下書き用の利便」であり、使う場合は
  **必ず警告エコー**を出して黙ってライブにしない。
- 静的/ライブの**行単位混在を許さない**: 列があれば全行必須（空欄はエラー停止）、
  なければ列ごと省略。「一部だけ静的」は再現性の観点で最悪（どの行が動くか
  わからない）。
- 静的化した値には **as-of（取得日・出所）を記録**する（CSVにコメントを置けない
  形式なら docs 側に表で持つ）。値の鮮度はデータの一部。
- 再現性の修正は**二連続生成の完全一致**（浮動小数まで）で実証する。
- 上場廃止・TOB銘柄（yfinance 404）はライブ経路では永久に欠損する —
  静的列はフォールバックではなく**必須の正本**になるケースがある（LightWorks 4267）。

## 複数手法平均への無意味値混入 — 「式は正しいのに答えが嘘」型 (2026-06-12)
**Mistake (構造):** Executive Summary の Target Mid = AVERAGE(4手法) が、各手法の
「適用可能性」を確認せずに機械平均していた。赤字企業では PER 法が
「PER中央値 × マイナス純利益」となり、数式としては正しく計算された無意味値
（5246: -534、4192: -11）が平均を汚染（5246: 真値644 → 349 に歪み、推奨も
BUY → SELL に反転していた）。recalc も整合チェックも通る — サイレント
フォールバックと同型の「エラーなく間違う」パターンの集計版。

**Rule for myself:**
- 複数手法を集計（平均・中央値・レンジ）する箇所では、各手法に**前提条件
  （適用可能性ガード）**を必ず定義する: 倍率法は「中央値倍率 × 対象指標」の
  対象指標が正（PER→純利益>0、EV/EBITDA→EBITDA>0）であること。
- ガードに引っかかった手法は **テキスト "N/A" で出力**する（AVERAGE/MIN/MAX は
  テキストを無視するので、集計式は据え置きで有効手法のみの平均になる）。
  `=NA()`（エラー値）は集計ごと壊すので使わない。
- **黙って除外しない**: 除外が発生したら、その旨を成果物上に注記セルで明示する
  （Exec Summary B20）。下流の `% vs price` セルは `IF(ISNUMBER(...))` で防護。
- 修正の検証は3方向: ①対象銘柄（赤字）で除外＋注記が出る、②黒字銘柄で
  従来どおり含まれ値が不変（2359で確認）、③下流消費者（market_analysis の
  `_num('C28')` は非数値→None で安全）への影響を grep して確認する。
- 「モデルが黒字/赤字どちらか」は推測せず実セル（Comps!C22）で確定する —
  4192 は黒字と思われていたが実際は赤字（NI -17mn）で、回帰前提が崩れていた。

## サイレントフォールバック禁止 — 「完走 ≠ 反映」事故 (2026-06-12)
**Mistake (構造):** パイプライン全体に「黙ってデフォルトに落ちる」点が多数あった:
(1) overrides の未知キー・ネスト構造・独自シナリオ名は無検出で消える、
(2) comps CSV 不在は print のみで comps=[] のまま完走、
(3) market_analysis の `float(C26 or 0)` は未recalcモデルで WACC=0 のまま完走、
(4) `.txt` 拡張子の comps ファイルは契約パス `.csv` にヒットせず黙殺。
結果、「実行完走・recalcエラーゼロ」なのに 5246 モデルに 4192 の comps が混入し、
旧形式 overrides の WACC が無視される事故が起きた。**投資判断に使う数値で最悪のパターン
は「エラーで止まる」ではなく「エラーなく間違う」。**

**Rule for myself:**
- 入力契約（キー名・パス・拡張子・構造）は**実行前にバリデートしてエラー停止**する。
  デフォルトへのフォールバックは明示フラグ（--allow-unconfirmed / --no-comps）でのみ許す。
- `x or 0` / `dict.get(k, default)` を外部入力・数式キャッシュに使うときは、
  「None がここに来るのはどういう状態か」を必ず問う。未recalc・契約違反なら raise。
- 生成物の重要値（WACC入力・comps名）は完了時にエコーして目視照合可能にする。
- 「過去の数値が壊れていたか」は推測せず、**生成済み xlsx の実セルを読んで事実確定**する
  （今回: 4192 は正しかった、5246 の comps/terminal/exit は誤りだった、と確定できた）。
- 契約は docs/overrides_schema.md が正本。新銘柄の overrides を推測で書かない。

**副教訓:** 死にキーに見えても間接消費がある（scenarios の itemized NWC キーは
`nwc_items[*].scenario_key` 経由で消費）。キーを「未使用」と断ずる前に間接参照を grep する。

## EDINET document search — budget vs. multi-season fallback (2026-06-04)
**Mistake:** Added a `MAX_API_CALLS=60` budget to `get_document_ids` to stop the
"hang", but 60 was exhausted during the early filing seasons before reaching the
ticker's actual season. This broke 4192 (a December-FY filer found in season 2),
which had worked before — a regression.

**Root pattern:** A global API-call cap interacts badly with a sequential
multi-season fallback: legitimate tickers that file in a *later* season need to
"waste" calls scanning earlier seasons first. A cap sized for the *common* case
silently breaks the *uncommon-season* case.

**Rule for myself:**
- When adding a safety budget on top of an existing sequential-fallback search,
  size it to cover a FULL scan of all fallback branches (here: 4 seasons × ~7
  years ≈ 310 calls → set 400), not just the expected fast path.
- The real fix for a slow search is to make the *right* branch run first
  (dynamic season from `fiscal_year_end_month`) + short-circuit once any result
  is found — not to clamp the cap aggressively.
- Always run the regression ticker (4192) immediately after touching shared
  search code, before declaring the target ticker fixed.

## Overrides schema must match the template's hard-coded contract
**Observation (5246):** The overrides file used custom scenario names
(Bull/StrongBull/Bear/StrongBear) and a nested `wacc_inputs` block, but
`dcf_comps_template.py` only reads the 5 canonical names
(`Base/Upside/Management/Downside 1/Downside 2`) and top-level WACC keys
(`risk_free`, `beta`, `erp`, ...). Result: only `Base` was applied; the custom
scenarios and WACC inputs were silently ignored.

**Rule:** When an overrides file doesn't visibly take effect, check it against
the template's hard-coded `SCENARIO_NAMES` and the top-level config keys —
mismatched names are dropped without error. Surface these to the user rather
than renaming (which would misrepresent their intent).

---

## 2026-08-23 — 「ラベルを `=` で始めない」を自分で踏んだ(2962)

**何が起きたか**: Adjustments Log の「元の値」列に `='Comps Analysis'!C28 => -69` と書いた。
openpyxl はこれを**数式として保存**し(`<f>'Comps Analysis'!C28 =&gt; -69</f>`)、Excel が
ブックを一切開けなくなった(`Workbooks.Open` が com_error -2146827284)。openpyxl では
読めるため、検証を openpyxl だけで済ませていると**気づけない**。

**なぜ踏んだか**: 手順書 §5-5-1 と §7-1 に明記されている罠なのに、「ラベル」= 表示用の
見出しだけの話だと読んでいた。実際は**セルに入る全ての文字列**が対象。「元の値」「変更前」
のような、数式を記録するための列がいちばん危ない。

**ルール**:
1. openpyxl でセルに文字列を書く箇所は、**書き込みループ内で `assert not str(v).startswith('=')`**
   を必ず入れる。1〜2セルだけ手で確認するのでは足りない(今回まさに2セルだけ assert していて、
   ループ内の6行×6列は素通りした)。
2. 数式を記録したいときは `formula: ...` / `旧: ...` のように**必ず非 `=` の接頭辞**を付ける。
3. **手修正の直後に Excel COM で開けることを確認する**。openpyxl で読めることは無傷の証明に
   ならない。`recalc_excel_com.py` が com_error で落ちたら、まず疑うのはこの罠。
4. 切り分けは「行単位で消して Excel Open を試す」二分探索が速い。今回は6行→2行→F列と
   3ステップで特定できた。

**手修正の順序**: 生成 → (recalc なしで) openpyxl 手修正 → recalc → validate が安全。
Excel が保存し直したブックを openpyxl で書き戻す往復を減らせる。

---

## 2026-08-26 — 未コミットのファイルを読まずに上書きした (tasks/todo.md)

**何が起きたか**: テンプレート修正作業の冒頭で `tasks/todo.md` に新しい計画を書いた。
このファイルには前セッション(5726)の**未コミットの作業ログ**が入っていて、`git diff` の
統計 (329行変更) は見ていたのに中身は先頭40行しか読まずに全文を上書きした。
末尾の「Review (2026-08-26)」本文は git にも無く、復元できなかった。

**なぜ踏んだか**: 「todo.md はタスクごとに書き換えるファイル」という思い込み。
`git status` で ` M tasks/todo.md` を見た時点で、HEAD にも無い内容が working tree に
だけ存在することは確定していた。

**ルール**:
1. **上書きの前に `git status` を見る。` M` が付いていたら working tree の内容が唯一の
   コピーである** — HEAD に戻しても復元できない。読むか、退避するか、追記にする。
2. 既存ファイルへの Write は、全文を読んでいないなら**追記**にする。特に
   `tasks/todo.md` `tasks/lessons.md` のような「積み上げ型」のファイル。
3. 作業前に `git stash create` か `cp` で退避すれば1コマンドで守れる。



---

## 決算期末の直後に評価すると stub_fraction が二重にズレる (2026-08-29, 3441)

**何が起きたか**: 3441 山王は**7月期決算**で、分析日は 2026-08-29 —— FY2026/7期は
7/31 に既に終了しているが本決算は9月中旬まで出ない、という「期は終わったが数字は無い」窓。
generate_dcf.py の自動経路は EDINET の最新中間報告(半期報告書 = 2Q)ラベルから
`stub_fraction = (12 - 6)/12 = 0.50` を導く。これは「**当期の残り半年**」の意味だが、
その当期は既に終わっている。放置すると Y1 の FCF が 0.5年で割り引かれ、実際には
11ヶ月先にある FY2027/7 の FCF が半年後に入ってくることになり、**全年度の割引が
半年ぶん過小**になる(Target が数%上振れる)。

**なぜ静かに壊れるか**: 0.50 も 0.92 も「もっともらしい」数字で、Executive Summary の
どこにも矛盾が出ない。validate_output のチェックにも stub の意味論を見るものは無い
(C19 の値が記録と一致するかは見るが、その値が正しいかは見ない)。

**Rule for myself**:
1. 非3月期銘柄では `fiscal_year_end_month` を入れて終わりにしない。**分析日が会計年度の
   どこにあるかを必ず手で数える**。`stub_fraction = (基準年の次のFY期末 − 分析日) / 365`。
2. 「直近本決算FY」と「基準年」を混同しない。**期末は過ぎたが未発表**という窓では、
   基準年は「実績Qの積み上げ + 残Qの推定」で自分で作る(本件は9ヶ月実績 + Q4推定)。
   その場合 `base_year_revenue` / `base_year_cogs` / `hist_years` の最終列 / `ltm_revenue` /
   `projection_start_fy` / `stub_fraction` / `stub_months_elapsed` を**セットで**上書きする
   —— 1つでも自動値のまま残すと基準年の定義が2つ混在する。
3. 推定で作った基準年の列ラベルには **`E` を付ける**(`FY2026/7E`)。Financial Statements は
   `hist_years` をそのまま見出しにするので、実績と推定が同じ見た目で並ぶのを防げる。

## 自己株控除後の株数は「BPS × 株数 = 純資産」で検算できる (2026-08-29, 3441)

**何が起きたか**: プロンプトは「自己株式の株数が未取得。暫定で発行済500万株を使用」と
指定していた。しかし自己株控除後の株数は、開示済みの数字だけから**3本の独立した突合**で
確定できた: (1) EDINET有報の `NetAssetsPerShareSummaryOfBusinessResults`(BPS)× 株数 =
開示純資産 → BPSの分母が自己株控除後であることが確定、(2) yfinance の
`sharesOutstanding` × `bookValue` = 直近BSの純資産、(3) 自己株簿価の期間増分 ÷ 株数増分 =
その期間の株価水準として妥当か。結果 4,241,897株(自己株758,103株)。
発行済 5,000,000株のままだと時価総額が **+17.9%**、EV が +14.8% になり、
このモデルの主役である逆算DCFの答え(「価格は何%の定常化を織り込んだか」)が
84% ではなく 97% になる —— 結論が「まだ織り込まれていない」から「ほぼ織り込み済み」に変わる。

**Rule for myself**:
1. 株数が「暫定」と言われたら、まず **BPS × 株数 = 純資産** の恒等式を試す。
   有報のサマリー(主要な経営指標等の推移)には BPS も純資産も必ずある。
2. `TotalNumberOfSharesHeldTreasurySharesEtc` / `NumberOfIssuedSharesAsOfFiscalYearEnd...`
   は EDINET の XBRL に素で入っている。自己株「簿価」しか無くても、株数タグを直接引ける。
3. 自己株は増えるとは限らない(本件は 674,400 → 656,800 → 758,103 と一度減っている)。
   簿価から株数を割り戻すときは**平均取得単価ではなく期間の増分**で割ること。
4. 株数はモデルの結論に直結する**唯一の分母**。プロンプトの暫定値をそのまま使うのは
   「自己株控除未反映」と注記すれば済む話ではない —— 取れるなら取る。


---

## 数式内の文字列リテラルは255文字まで — 超えるとブックがExcelで開けなくなる (2026-08-29, 278A)

**何が起きたか**: 278A の初回生成で `recalc_excel_com.py` が com_error で落ちた。openpyxl は
問題なく読み書きでき、`validate_output.py` も PASS 14 / FAIL 0 を返す。壊れているのは
**Excelがファイルを開けない**という一点だけで、生成パイプラインのどの検査にも引っかからない。
シート単位の二分探索で `Executive Summary` を特定し、さらに行単位で B24/B25 —— つまり
`investment_thesis` を TEXT 連結数式に変換したセル —— に絞り込んだ。

**原因**: Excel は**数式の中の文字列定数を255文字までしか受け付けない**。最小再現で確認:
リテラル255文字は開ける、256文字は開けない。テンプレート
(`dcf_comps_template.py` の NARRATIVE_TOKEN 処理)は「1セル8,192文字」での分割は実装しているが、
「1リテラル255文字」での分割はしていない。トークン(`{price}` 等)を含む thesis/risks 行の、
**トークンとトークンの間のテキストが255文字を超えた瞬間**にブックが壊れる。
3441 や 5726 で起きなかったのは、たまたま各セグメントが短かったからにすぎない。

**なぜ静かなのか**: 生成は成功する。openpyxl で開ける。validate_output は数式の**文字列**を
見るので構造チェックは全部通る。唯一の症状が Excel COM の com_error で、これは
「Excelが起動していない」「別プロセスがファイルを掴んでいる」といったよくある環境要因と
区別がつかない —— 実際、最初はそちらを疑って Excel プロセスを kill してから再試行した。

**Rule for myself**:
1. thesis / key_risks を書くとき、**トークンで区切られた各セグメントを255文字以内**に収める。
   トークンを含まない行は素のテキストセルになるので上限32,767文字で問題ない。
   書く前に機械的に検算する: `re.split(r'\{[a-z_]+\}', line)[::2]` の各要素の len を見る。
2. `recalc_excel_com.py` が com_error で落ちたら、**環境要因を疑う前に既知の良品を1つ recalc して
   みる**。良品が通ってその銘柄だけ落ちるなら、原因はブックの中身である。
3. 切り分けは**シート単位 → 行単位の二分探索**が速い(openpyxl で他シートを削除して Open を試す)。
   今回は 8シート → Executive Summary → B24:B26 → B24/B25 の3ステップで特定できた。
4. テンプレ修正候補: リテラルを255文字ごとに文字列連結演算子で分割して書き出す。
   後方互換(255文字以下のリテラルは分割不要なので既存銘柄の出力は不変)。

## 生成後スクリプトの実行順序は固定 — add_segment_bridge を後から再実行しない (2026-08-29, 278A)

**何が起きたか**: `data/segments/278A_segments.json` の注記を1行直したかったので
`add_segment_bridge.py` だけを再実行した。その時点で `Adjustments Log` は
`fill_adjustments_log.py` によって38件が記入済みだった。`append_open_items()` は
「列Bが空になる行」まで下に歩いて open_items を書き込む実装なので、記入済みの表を通り過ぎ、
表と Pipeline Metadata バンドの間の**空行に書き込み、その次の行 = `Pipeline Metadata` の
見出し行を上書き**した。結果、validate_output がメタデータを読めず PASS 18 → SKIP 6 / PASS 12 に落ちた。

**Rule for myself**:
1. 生成後スクリプトの順序は **generate_dcf → add_segment_bridge → fill_adjustments_log →
   recalc_excel_com → validate_output** で固定。この順序に「戻る」ときは、
   **途中から再実行せず必ず generate_dcf からやり直す**。
2. 設定ファイル(segments / adjustments の JSON)を直したら、xlsx は**作り直す**。
   xlsx は生成物であって編集対象ではない。`models/` は上書き可という規約はそのためにある。
3. 症状の見分け方: `fill_adjustments_log.py` が
   `Pipeline Metadata band not found - refusing to guess row numbers` を出したら、これが起きている。
   スクリプト側は「推測を拒否して止まる」正しい振る舞いをしているので、素直に再生成する。

## データ保存先を外出しするときは「DBに書かれたパス」まで見る (2026-08-31, screener)

**何が起きたか**: C: の空きが約5GBになったので screener の保存先を D: へ移す作業。
コード側のパスは `common.py` に集約されていたので `DATA_ROOT` を1箇所足すだけ…と思ったが、
`filings` テーブルの `path / pdf_path / xbrl_path` が **リポジトリルート相対**
(`screener\data\raw\tdnet\...`) で保存されていた。ファイルを移した瞬間に
353行すべてが行き先を失う。`os.path.relpath(dest, C.ROOT)` で書き、
`os.path.join(C.ROOT, ...)` で読む対称な実装だったので grep しないと気付けない。

**Rule for myself**:
1. 保存先を外出しする作業では、**コードのパス定数だけでなくDBに永続化されたパスも
   grep する**。`relpath` / `abspath` / `join(.*ROOT` を必ず検索する。
2. 永続化するパスは **データルート相対**にする。リポジトリ相対でも絶対でもない。
   ドライブや母艦が変わっても DB を書き換えずに済むのはこの形だけ。
   (`C.store_path()` / `C.full_path()` を経由し、各モジュールで組み立てない)
3. 移行スクリプトは **dry-run を既定**にし、書き換え後のパスが実在するか全件確認してから
   apply する。1件でも解決できなければ apply を拒否する ——
   壊れたパスで書き換えると「動いているのに中身が空」という一番気付きにくい壊れ方をする。
4. 検証は「DB→ディスク」だけでなく **「ディスク→DB」の孤児チェック**も行う。
   今回は 437ファイル全てが参照済み・孤児0で移行完了を確認できた。

## Git Bash のヒアドキュメントはバックスラッシュを畳む (2026-08-31, 環境)

**何が起きたか**: `cat > f.py <<'PYEOF'` (クォート付き=リテラルのはず) で
`"screener\data\\"` と書いたのに、ファイルには `screener\data\` が落ちた。
Python が `\d` `\x` を unicode escape として解釈して SyntaxError。
README でも `raw\tdnet` の `\t` がタブ文字になって表が壊れた。

**Rule for myself**: **Windows パスやバックスラッシュを含むファイルを書くときは
heredoc を使わず Write/Edit ツールを使う**。heredoc は「バックスラッシュを含まない
テキスト」専用と考える。書いた後は `cat -A` か Read で実際のバイトを確認する。

## 2026-08-31 — ファイルを上書きする前に中身を読む

**やらかし**: `tasks/todo.md` に計画を書くとき、既存の内容を読まずに丸ごと上書きした。
HEAD の内容は `git show HEAD:tasks/todo.md` で復元できたが、**コミットされていなかった
作業コピーの差分は失われた**。

**ルール**: 既存ファイルへの書き込みは、まず読む。読んだうえで
「追記」か「置換」かを決める。`tasks/todo.md` `tasks/lessons.md` のような
累積ログは原則 **追記**。丸ごと上書きしてよいのは自分が同一セッションで
作ったファイルだけ。

## 2026-08-31 — 「認証が通らない」を認証だけの問題だと決めつけない

**やらかし**: J-Quants が全面 403 になったとき、リフレッシュトークンの文字数を数えて
「トークンが不正」と結論づけ、README にもそう書いた。実際は **API の V1 が
2026-06-01 に終了** していた。トークンをいくら取り直しても通らない類のもの。

**ルール**: 4xx が **全エンドポイントで一様に** 出るときは、資格情報より先に
**API のバージョン/提供状況を公式ドキュメントで確認する**。個別エンドポイントだけが
落ちるなら権限・パラメータ、全部落ちるなら世代・契約・提供終了を疑う。

**ついでの教訓**: メジャーバージョン移行では認証だけが変わるとは限らない。
今回はパス・レスポンスのキー・項目名・レートリミットの単位まで変わっていた。
「認証だけ直して build」は必ず空振りする。移行表を先に作ってから手を動かす。

## 2026-08-31 — tasks/ 上書き事故【2回目】: 注意ではなく仕組みで止める

**やらかし**: `tasks/todo.md` を中身を読まずに丸ごと上書きした。同種の事故は
これが2回目。1回目のあと「読んでから書く」というルールを書いたが、それでは
止まらなかった。**注意で守れなかったルールは仕組みにする**。

**根本原因（1回目の分析が浅かった点）**: 事故の経路は Write ツールではなく
**Bash のリダイレクト**だった。Write ツールには「読まずに既存ファイルを
上書きすると失敗する」ガードが最初から効いているが、`cat > tasks/todo.md`
はそのガードを丸ごと迂回する。「読んでから書く」と自分に言い聞かせるだけでは、
ツールを変えた瞬間に無力になる。

**入れた仕組み**: `.claude/hooks/protect_tasks_append_only.py` +
`.claude/settings.json` の PreToolUse フック（matcher: `Write|Bash|PowerShell`）。
tasks/ 配下の**既存**ファイルに対して、

- Write ツールでの上書き
- 切り詰めリダイレクト `>`（`>>` は許可）
- `tee`（`-a` 無し）/ `sed -i` / `Set-Content` / `Out-File`
- `cp` / `mv` / `Copy-Item` / `Move-Item` の宛先

を deny し、「まず全文を Read → 追記か Edit で差分を当てる」よう指示を返す。
新規ファイル作成・追記・読み取りは通す。10ケースでパイプテスト済み。

**運用ルール**: tasks/ は追記専用。どうしても作り直すときは、先に
`git diff -- tasks/` で未コミット差分を確認し、ユーザーの明示の了解を取る。

**一般化**: 「次から気をつける」で終わる再発防止は、2回目が起きた時点で
失敗が確定している。2回目を見たら、ルールではなく実行環境側で不可能にする。

## 2026-09-01 — tasks/ 上書き事故【3回目】: 仕組みの「有効範囲」を確かめていなかった

**やらかし**: `cat > tasks/todo.md` で 440 行の todo.md を全文上書きした。2026-08-31 に
「注意ではなく仕組みで止める」としてフックを入れた、その翌日にである。

**なぜ仕組みが素通りしたか**: フックは**リポジトリの** `.claude/settings.json` に
書いてあった。今回のセッションは作業ディレクトリが `<HOME>`（ホーム）で、
リポジトリはその下のサブディレクトリにすぎない。プロジェクト設定は cwd から解決される
ので、**フックは一度も読み込まれなかった**。deny も警告も出ない —— 何も起きないまま
通る。前回「注意で守れなかったルールは仕組みにする」と書いたが、その仕組み自体の
**有効範囲を検証していなかった**。入れた気になっていただけ。

**復元**: 今回は commit 済みだったので `git checkout -- tasks/todo.md` で全文復元し、
追記(`>>`)でやり直した。差分は +35 行のみ、欠損なし。2026-08-26 の事故（未コミットで
復元不能）と違って運が良かっただけで、手口は同じ。

**入れた仕組み（2段目）**: 同じフックを **ユーザーレベル** `~/.claude/settings.json`
にも登録し、スクリプトを `~/.claude/hooks/` に置いた。cwd に関係なく全セッションで効く。
登録後、`tasks/` の既存ファイルへ `>` を実行して**実際に deny されることを確認済み**
（プローブファイルで検証し、削除した）。

**一般化**: 防御を入れたら「入れた」で終わらせず、**それが効く条件と効かない条件を
1回ずつ実測する**。設定ファイルには必ずスコープ（ユーザー / プロジェクト / ローカル）
があり、スコープを間違えた防御は、無いのと同じどころか「守られているつもり」がある分
危険。同じ事故の3回目は、たいてい対策そのものではなく**対策の適用範囲**が原因。

## Excel の「数式内文字列リテラル255字」制限 — 生成物が開けなくなる (2026-09-03 / 1433)

**Mistake (構造):** thesis/risks にトークン({per} 等)を含む行は TEXT 連結数式に変換される。
テンプレは「1セル 8,192字超なら文単位に分割」するが、**Excel の実際の制約は
『数式内の1つの文字列リテラルが255字まで』**。プロンプト原文のテーゼ(320字・324字の
連続リテラル)がこれを超え、生成された .xlsx を **Excel が開けなくなった**
(COM Workbooks.Open が HRESULT 0x800A03EC で失敗)。openpyxl は書き込み時に検知せず、
recalc が失敗するので validate_output は FAIL ではなく **SKIP 5件**で PASS を返した。
つまり「壊れているのに VERDICT: PASS」。

**Rule for myself:**
- 数式を組み立てるときは、**出力先アプリの制約を長さ2種類で持つ**: セル全体(32,767字 /
  数式8,192字)と**リテラル1本(255字)**。長い散文をトークンで連結する設計では後者が先に来る。
- 「生成できた」は「開ける」ではない。**recalc の失敗を SKIP で流さない** — 検証が
  SKIP に落ちたら、その原因を潰すまで PASS と読まない(SKIP は PASS ではない)。
- 原因特定は二分探索で機械的にやる: シート単位 → 数式セル単位 → **対照実験で境界を確定**
  (254字→開ける / 256字→開けない)。推測で「たぶん長さ」と書かない。
- 汎用テンプレの制約に当たったら、①今回の回避策、②テンプレ側の恒久修正案、を両方
  Adjustments Log と報告に残す(手順書v2 §6-6)。

## パイプラインの自動取得が「静かに1期しか取れない」ことがある (2026-09-03 / 1433)

**Mistake (構造):** `get_document_ids` の adaptive_search は、最初に見つけた提出日の
**±5日**でしか過去年を探さない。1433 の有報提出日は 4/16 → 4/25 → 4/26 → 4/28 → 4/28 と
年により最大12日ずれるため、**5期中1期しか取れず、しかもエラーにならない**。
Financial Statements は3期空欄のまま完走する。

**Rule for myself:**
- hist が想定より短いときは「この会社は上場が新しい」と決めつけず、**提出日レンジを
  総当たりして実在を確認**する(4/1-5/20 の走査で4本すべて発見できた)。
- 遡及修正に注意: FY2022/1 は原表(販管費749.3/営業利益607.9)と翌期比較列
  (868.4/488.8)が食い違う。**5年サマリー表(主要な経営指標等の推移)が公式値**なので
  そちらと突合してから採用する。

## バッチDCF生成(大手90銘柄, 2026-09-05)で判明したパイプラインの罠

1. **beta のクランプは範囲外を無言で 1.0 に置換する**（`templates/dcf_comps_template.py:1819`、[0.6, 1.75]）。
   日本の大型ディフェンシブ銘柄の実測 beta は 0.0〜0.4 に集中しており、そのまま渡すと**全部 1.0 になって WACC が 2〜4pt 跳ね上がる**。
   → overrides で `max(0.6, min(1.75, 実測))` を明示指定し、「0.60 は実測値ではなくテンプレが受け付ける最小値」と Adjustments Log に書く。

2. **EDINET は古い年度の営業利益・COGS を 0 で抽出することがある**。9銘柄中4銘柄で発生し、
   `Financial Statements!<列>16 = #DIV/0!`（営業利益YoY成長率の 0 除算）で validate FAIL 1 になる。
   → `hist_years` を直近4期に置換し、P/L 5系列(revenue/operating_income/net_income/cogs/sga)を供給する。
   **`hist_ocf`/`hist_cash`/`hist_debt`/`hist_capex`/`hist_depreciation` は供給しない** —
   Step 4.6 の `align_hist_series_to_years()` が最終 hist_years に年度キーで再マッピングするため、EDINET を一次ソースのまま使える。

3. **EDINET が有利子負債を1年も抽出できないと net_debt が静かに 0 になる**（2801 キッコーマンで発生、実際は −50,341）。
   生成ログの `[FS align] ... [ocf+cash]`（debt が無い）を必ず読むこと。

4. **EDINET の探索窓が最新の有価証券報告書を捕捉しないことがある**（2802 味の素・2502 アサヒ）。
   半期報告書だけ取れて本決算が1年古いまま進む。→ `fundamentals_fy<YYYY>` で最新期を注入する。

5. **会社業績予想は EDINET XBRL からも fetch_tanshin からも1件も取れなかった**（9銘柄すべて）。
   Management シナリオが常に Base と同一になる。銘柄固有ではなく構造的な取得失敗。

6. **exit_multiple にマージン品質調整を入れてはいけない**。EV/EBITDA は既に資本集約度を織り込んでいるため、
   営業利益率による上方調整は資本集約型で逆方向に働く（9022 JR東海: 実測 7.4x に対し調整式が 16.3x を返した）。
   → **素の Peer 中央値 EV/EBITDA** を使う。

7. **連結内に銀行を持つ持株会社は財務比率では検出できない**。9433 KDDI は通信会社に見えるが、
   有報 XBRL の連結BSに `ShortTermLoansForFinancialBusinessCAIFRS` / `ShortTermDepositsForFinancialBusinessCLIFRS` /
   `CallLoanAndBillsBoughtCAIFRS` が独立科目で存在し、金融事業が総資産の33.6%を占める（auじぶん銀行）。
   → 生成前に `batch/fin_business_screen.py` で XBRL 要素名をチェックする。

8. **yfinance は最新期のキャッシュフロー計算書を収録していないことがある**。comps CSV で
   `EBITDA = 営業利益(最新期) + D&A(前期)` という年度混在が静かに起きる。検出して開示すること。

9. **上記2〜4の根本原因は `scripts/generate_dcf.py:66` の1行**:
   `def _val(data_dict, key, default=0)` が欠損を None ではなく 0 に落とす。
   hist_* 全系列と `net_debt`(169行) がこれを通るため、EDINET の抽出漏れが**警告なしでゼロとして通過**する。
   修正は影響範囲が広い（`hist_cogs` の逆算・`core_ebitda`・Reverse DCF の op0/peak 導出等）ため、
   バッチ中は変更せず overrides 側で回避した。

10. **core_ebitda が壊れても validate は FAIL 0 のまま**。Comps の「自社 Financials」EBITDA と
   Via EV/EBITDA 参考株価は comps CSV ではなく config の `core_ebitda` を使うため、
   EDINET の減価償却費が取れないと参考株価が N/A や**負値**になる（4502 武田で −2,761円）。
   → `batch/check_core_ebitda.py` で全銘柄を機械照合する。Comps は Target 不算入なので Target は動かない。

11. **EDINET 破損は発生を待たず予防的に補完した方が速い**。19銘柄中8銘柄で #DIV/0! が出たあと、
   残り5銘柄に予防的補完（hist_years を4期に置換 + P/L 5系列供給）を適用したところ**5銘柄すべて初回合格**した。

12. **yfinance の市場データ取得が失敗するとテンプレのプレースホルダ（株価1,000円/株数10,000,000株）に落ち、
   validate FAIL 0 のまま通る**（4568 第一三共で Target 294,427円 / BUY +293% が出た）。
   生成ログに `Successfully fetched` 行が無いのが唯一の手がかり。
   → `batch/check_market_data.py` で全生成物の株価・株数をキャッシュと機械照合する。
   予防としては overrides に `current_price` / `shares_outstanding` を明示指定する（再現性も上がる）。

13. **EDINET 破損は「発生したら直す」より「全銘柄に予防適用」の方が速い**（追補2 §F-2 で必須化）。
   予防適用した10銘柄で #DIV/0! はゼロ。`batch/preemptive_block.py` が
   hist_years（直近4期）+ P/L 5系列 + base_year_* + core_ebitda + 株価・株数を機械生成する。
   **CF/BS 5系列は供給しない** — Step 4.6 が年度キーで再マッピングするため EDINET を一次ソースのまま使える。

## 14. `generate_dcf.py` は `--force` なしだと**黙って再生成をスキップし、それでも exit 0 を返す**

**症状**（第6セッション、追補5 §V-1 の 6752 再生成で遭遇）

overrides を書き換えて `python scripts/generate_dcf.py 6752` を実行したところ、Step 7 で

```
WARNING: models\6752_DCF_Model_20260905.xlsx already exists.
 Use --force to overwrite, or rename/move the existing file.
```

と出て**ワークブックを生成せずに終了**した。しかし **exit code は 0**。
さらに悪いことに、続けて `models/6752_DCF_Model_20260905_validation.txt` を読むと
**古いファイルに対する `VERDICT: PASS` がそのまま残っている**。

**なぜ危険か**

再生成フローで `exit 0` と `validate PASS` だけを見て `done` を打つと、
**overrides の変更が1行も反映されていないモデルを「再生成済み」として確定してしまう**。
追補5 §U のような「設計をやり直す」系の指示では、成果物が変わっていないのに
「やり直した」と報告する最悪の failure mode になる。

**ルール（自分向け）**

1. **既存モデルを作り直すときは必ず `--force` を付ける。**
2. 再生成後は **exit code と validate だけを信用しない**。
   `models/<t>_*.xlsx` の **mtime が overrides の mtime より新しいこと**を必ず確認する。
3. バッチ全体でも同じ監査を回す（overrides の mtime > model の mtime なら stale）。
   第6セッションで done 39銘柄を監査した結果、stale は再生成中の 6752 のみで、
   過去セッションの取りこぼしはゼロだった。
4. 生成ログを `> file` にリダイレクトすると Python が stdout をバッファするため
   **途中経過が 0 バイトに見える**。進捗確認は成果物の mtime で行う。

**根本原因（バッチ後の修正候補に登録）**

`scripts/generate_dcf.py` の Step 7 は「出力済みファイルの保護」を意図した安全弁だが、
(a) 終了コードで失敗を伝えず、(b) 後続の validate を止めないため、
**保護のつもりが silent no-op になっている**。
`--force` 未指定で既存ファイルがある場合は **非ゼロ終了して validate に進まない**のが正しい。

## 15. validate チェック9「ターミナル capex/D&A」は `capex_pct ÷ da_pct` で判定される

**症状**（第6セッション、part1 の6銘柄で遭遇）

`overrides_validator` が
`terminal capex / D&A = 1.18x with terminal_growth 1.00% (outside [0.90, 1.15])`
と警告したので、`capex_direct.projections` の**最終年を縮めた**。
1.05倍になるようスケールしたのに、**警告の比率が1ミリも動かなかった**（1.18x のまま）。
投影の最終年を 25,000 → 12,500 と半分にしても変わらない。

**正しい理解**

この比率は **`capex_pct ÷ da_pct`**（＝ワークブックの C5 ÷ C18、ターミナル年の前提セル）で計算されている。
`capex_direct.projections` は**明示予測5年分**を支配するだけで、ターミナル価値の判定には入らない。

- 6506: 0.0460 / 0.039 = 1.179 → 警告の 1.18x に一致
- 7751: 0.0570 / 0.052 = 1.096 → 警告なし

**ルール（自分向け）**

1. ターミナル整合を取るには **`capex_pct = da_pct × 1.05`** を置く。投影の最終年ではない。
2. あわせて **Y5 の投影 ≈ Y5売上 × capex_pct** にしておく（明示期間とターミナルの段差をなくすため）。
3. **警告メッセージの数値が動かないときは、自分が触っているレバーが судьи の見ているレバーと違う**。
   同じ修正を2回繰り返す前に、判定式が何を読んでいるかを確認する。

## 16. 「gate 通過」を一度確認したら終わり、にしない — 後からデータを触ると壊れる

**症状**（第6セッション）

done 45銘柄に `check_core_ebitda.py` を掛け直したら **43/45**。2503 キリンと 4183 三井化学が MISMATCH だった。
どちらも過去セッションで `done` を打った銘柄で、当時はゲートを通っていたはずのもの。

**原因**

yfinance のCF計算書に**直近期の D&A 行が無い**ため、
`build_comps.py` は前期D&Aへフォールバック（MIXED-YEAR として開示）して comps CSV の自社行を作る一方、
ワークブックは `da_pct × 売上` で別の D&A を使う。**2つの経路が別々の D&A を持つ。**
さらに後から Peer を足して comps CSV を再生成すると、自社行の値が変わってワークブックと食い違う。

**ルール（自分向け）**

1. **comps CSV を作り直したら、その CSV を使う全銘柄のゲートを掛け直す。**
   Peer を1社足しただけでも自社行が再計算される。
2. **`core_ebitda` は必ず明示指定する。**「ゲートが通っているから不要」は成り立たない
   （たまたま2経路が一致しているだけかもしれない）。
3. **直近期の D&A が yfinance に無い銘柄は珍しくない。**
   その場合は EDINET の `DepreciationAndAmortizationOpeCFIFRS` を取り、
   **前期の値が yfinance の前期と一致すること**を必ず確認してから採用する（同一定義の検証）。
4. セッション終了前に**全 done 銘柄へ全ゲートを掛け直す**。
   個別に通したことは、今も通っていることを意味しない。

## 17. 判定条件は「困っている事象」そのもので切る — 代理変数で切ると穴が開く

**症状**（追補6 §X で是正）

PGM と Exit の乖離が大きい銘柄への対処として、§Q（Exit > 25倍）と §U（分母がトラフ）という
**2つの独立したトリガー**を作った。結果、**どちらの条件も満たさないが乖離は大きい**銘柄が素通しになった。

- 6981 村田: 乖離 **4.55倍**（個別裁定した 6752 の 4.41倍より**大きい**）
- しかし直近OPM が 窓p25 を **わずか 0.56pt** 上回るだけで §U を外れ、Exit 17.20倍 ≤ 25倍 で §Q も外れる
- → **禁止されていたはずの中点平均がそのまま Target になっていた**

**何を間違えたか**

困っていたのは「**乖離が大きいこと**」なのに、トリガーを「倍率の高さ」「分母の位置」という
**代理変数**で書いた。代理変数は本来の事象と相関するだけで一致しない。相関が切れる領域に穴が開く。

**ルール（自分向け）**

1. **トリガーは困っている事象そのもので書く。** 分類（レジーム）は「どう対処するか」に使う。
   §X の形: 乖離 > 3.0倍 → 必ず裁定（トリガー）／ §Q・§U は「どちらの脚を落とすか」の判断材料（対処の分岐）。
2. **新しい閾値ルールを作ったら、必ず全件走査して「該当しないが症状は重い」群を数える。**
   1銘柄ずつ処理していると、穴は最後まで見えない。
3. 閾値の境界付近（0.56pt 差）で結論が正反対になるルールは、**その境界に意味があるかを疑う**。

## 18. 判定を反転させる変更は、反転したことを明示的に報告する

追補6 §X の遡及裁定で **6762 TDK が SELL(−5.7%) → BUY(+40.8%) に反転**した。
中点平均という「一見中立な処理」が、判定そのものを作っていた。

**ルール（自分向け）**: ルール変更を遡及適用したら、
**Target が動いた件数ではなく「判定（BUY/SELL）が変わった件数」を数えて報告する。**
金額の変化より、判定の反転のほうが利用者にとって重大である。
あわせて、反転した銘柄については**その判定がどれくらい頑健か**（本件では仮定Exit 18.10倍 が
Peerバンド 17.19–27.15倍 の下端にあること）を必ず併記する。

## 19. 日付を跨ぐとファイル名が変わり、ゲートは古いファイルを検査し続ける

**症状**（追補6 §Z の作業中に発覚）

`generate_dcf.py` は**実行日**をファイル名に埋める（`<code>_DCF_Model_<YYYYMMDD>.xlsx`）。
日付が変わってから再生成すると `_20260906.xlsx` という**別ファイル**が生まれ、
`_20260905.xlsx` が**古いまま残る**。3ゲートも state も `*_20260905.xlsx` を見ているため、
**古いファイルを検査して「clean」と報告し続ける**。§Z が塞いだ `--force` 事故と同じ構造。

**ルール（自分向け）**

1. **再生成後は必ず出力ファイル名を確認する。** 日付が変わっていたらバッチの基準日名にリネームする。
2. ファイル名の日付は**分析基準日**を表すものとして固定し、実行日はワークブック内の
   Pipeline Metadata ブロック（編集禁止）を正本とする。
3. `batch/regen.sh` を使う（`--force` 付き実行 + リネーム + 再validate をまとめてある）。
4. より一般に: **「同じ論理的成果物が2つのファイルに分裂しうる操作」の後は、必ず実ファイルを列挙して確認する。**

## 20. 「裏付け」を取りに行ったら逆を指していた、は隠さず書く

**症状**（追補7 §AA-2）

バンド不安定フラグ付き銘柄に Reverse DCF Block F の併記を義務づけた。目的は
「市場が何を織り込んでいるかで**判定方向を裏強する**」こと。ところが実際に取ってみると:

- **6981 村田**: Block F は SELL を独立に裏付けた（市場EVは2脚の両方より上）→ 狙いどおり
- **6762 TDK**: Block F は **BUY と逆**を指した（PGM脚では現値ですら1,518円割高）→ 狙いが不成立

**なぜ構造的に起きるか**

Block F は **PGM脚**を基準に計算される。したがって **Exit脚を採用した銘柄では、
Block F は構造的に Target と逆を向きやすい**。「裏強」は無条件には成立しない。

**ルール（自分向け）**

1. **裏付けを取りに行って逆が出たら、それは最も価値のある発見である。** 黙って落とさない。
   「一致したか / 不一致だったか」を明示する運用に変える。
2. 不一致のときは**「この判定は何に賭けているのか」を1文で書く**。
   6762 の場合は「Peer 並みの EBITDA 倍率への再評価に完全に依存しており、
   FCF 創出力が価格を正当化しているという主張ではない」。
3. 検証指標を設計するときは、**それが評価ロジックのどちらの脚から派生しているか**を先に確認する。
   同じ脚から出た指標で同じ脚を検証しても、独立した裏付けにはならない。

## 21. ルールを機械化したら、名指しされた銘柄以外も拾わないか必ず確認する

追補7 は 6762 を名指しして「バンド不安定フラグを付けよ」と指示した。
閾値（p75/p25 > 1.5）を実装して全件に当てたところ、**6981 も 1.55 で該当**した。

**ルール（自分向け）**: 指示が特定の銘柄を例示していても、
**ルール化した時点で対象は例示された銘柄ではなく閾値を満たす全件**である。
実装したら必ず全件走査し、**「名指し以外に何件増えたか」を報告する**。
例示だけに適用して済ませると、次のセッションで同じ穴が再発する。

## 22. 「何で測るか」と「何を禁止したいか」を一致させる — 3861 王子HDの穴

追補6 §X は「中点平均を禁止する」ルールだが、トリガーは
**WARN 11（PGM逆算"倍率" vs 仮定Exit"倍率"）の乖離**で測っている。
ところが **Target を決めるのは株価**である。

3861 王子HD は倍率乖離 **1.00倍**（WARN すら出ない）なのに、
PGM 116円 / Exit 563円 で **株価乖離 4.85倍**。禁止したかった中点平均がそのまま Target になっていた。

**なぜズレるか**: `株式価値 = EV − net_debt`。**レバレッジが高いと EV のわずかな差が株式価値で何倍にも増幅される**。
王子HD は net_debt 889,993 に対し株式価値が約 289,000 しかない。

**ルール（自分向け）**
1. 閾値ルールを作るときは「**禁止したい事象の単位**」と「**測っている量の単位**」が一致しているか確認する。
   倍率で測って株価を禁止する、はズレる。
2. **レバレッジが絡む指標は、EV ベースと株式価値ベースで別物になる**と最初から疑う。
3. 新ルールを実装したら、**そのルールが素通しした群**を必ず走査する（lesson 17 と同じ手順）。
   王子HD は「§X 非該当」の側を数えて初めて見つかった。

## 23. セクターが変われば同じモデルの壊れ方も変わる

part1（半導体・電子部品・医薬）では 45件中 **8件**が §X の裁定対象になったが、
part2（素材・建設・不動産・運輸）では 14件中 **0件**だった。

**理由**: 2脚が乖離するかは、そのセクターの **Peer 倍率が 1% 永久成長の DCF と両立する水準にあるか**で決まる。
part1 の Peer中央値は 17〜39倍で PGM逆算（3〜10倍）と重ならない。part2 は 4.8〜15.7倍で重なる。

**ルール（自分向け）**: 「このモデルは壊れやすい」という一般化をする前に、**どのセクターで壊れたのかを分けて数える**。
モデルの欠陥に見えたものが、実は「そのセクターの市場倍率が DCF と両立しない」という別の事実であることがある。
逆に、低倍率セクターで2脚が一致したことは**モデルが正しく動いている証拠**として記録する価値がある。

## 24. 指示の「定義」と「予想される影響の数値」が食い違ったら、定義を優先する

追補8 §AE-3 は rework の影響を「−0.06〜−1.14pt」（Target が下がる）としていたが、
同じ追補の **§AE-2 が定めた定義**（単調増加 = 直近水準の維持）で再算定すると
**+0.09〜+1.42pt（上がる方向）** と符号が逆になった。

原因は明確だった。§AE-3 の数値は**こちらが前セッションで提示した表**を引き写したもので、
その表は単調増加にも (直近+中央値)/2 を当てていた。つまり**古い解釈の数値**だった。

**判断の根拠**: (直近+中央値)/2 は**平均回帰そのもの**であり、
§Y は「トレンドに平均回帰を当てるのは統計的誤り」という理由で成立している。
単調増加にそれを適用すれば §Y は自己矛盾する。定義のほうが内部整合的だった。

**ルール（自分向け）**
1. 指示の中で**定義と数値が食い違ったら、定義がルールで数値は見込みである**と扱う。
2. ただし**黙って選ばない**。どちらを採ったか、なぜか、影響がどう変わるかを必ず報告する。
3. 特に「その数値は自分が前セッションで出したもの」だと気づいたら、
   **自分の古い出力が指示に混入した可能性**を疑う。指示は自分の報告を土台に書かれている。

## 25. 閾値の分母は「何が増幅されるのか」で決まる

追補8 §AD-2 の `net_debt / EV > 0.6` を、最初 EV = 時価総額 + net_debt で実装したら **0.545** となり
閾値を下回って N/A にならなかった。指示の例示は **0.75** だった。

分母を**モデルの株式価値**（Target × 株数）に変えると **0.76** で例示と一致した。

**なぜそちらが正しいか**: このルールの論点は
「**EV のわずかな差が"株式価値"で数倍に増幅される**」ことである。
増幅されるのはモデルが出す株式価値であって、市場が付けた時価総額ではない。
**分母は「増幅される側の量」でなければ、比率が現象を測っていない。**

**ルール（自分向け）**: 比率の閾値を実装するとき、
分子・分母がそれぞれ**現象のどの部分を指しているか**を言葉で書き下してから式にする。
指示に例示値があるなら、**必ず自分の実装で再現できるか確認する**（再現しなければ定義が違う）。

## 26. ディスク逼迫下で生成を続けると「緑の判定が出る壊れたファイル」ができる

**症状**（追補10 の移設中に §AO の検証で摘発）

C: の残量が 0.78GB の状態で DCF 生成を続けたところ、タイムアウトで Excel 再計算が中断され、
**9503 のワークブックが数式だけあってキャッシュ値のない状態**で残った。

validate はこれを **`FAIL 0 / VERDICT: PASS`** と報告した。実際は `SKIP 5 / PASS 14` で、
Target・判定・PGM・Exit がすべて `None`。**使えないファイルに緑の判定が出た。**

**`--force` サイレントスキップ（lesson 14）と同じクラスの事故**である:
「終了コードと VERDICT だけを見ると成功に見える」。

**ルール（自分向け）**

1. **`FAIL 0` だけでは足りない。`SKIP 0` も確認する。**
   SKIP > 0 は「数式値のチェックができなかった」＝再計算が完了していない可能性。
   → `batch/stale_check.py` の `assert_recalculated` として3ゲートに実装済み。
2. **重い外部プロセス（Excel COM）を伴う処理は、1銘柄ずつ実行する。**
   複数銘柄をループで回すとタイムアウトが再計算の途中を切る。実際に切れた。
3. **ディスク残量が閾値を切ったら、作業を止めて先に解決する。** 続行は成果物を壊す。
4. 移設・環境変更のあとの検証は**形式的な手続きではない**。今回は検証そのものが破損を見つけた。

## 27. 「容量の主犯」は指示の想定と違うことがある — 消す前に測る

追補10 は「容量の主犯は data/ 配下であることが多い」として data/・models/ の移設を指示していた。
実際に測ると:

| ディレクトリ | サイズ |
|---|---|
| **`tmp/`（EDINET展開キャッシュ）** | **4,883 MB** |
| `models/` | 4.7 MB |
| `data/` | 1.7 MB |

**指示どおり data/ と models/ を動かしても 6.4MB しか空かなかった。**
実体は `tmp/edinet_data/` で、しかも `.gitignore` 管理下の純粋な派生キャッシュだった。

**ルール（自分向け）**

1. **移設・削除の前に必ず実測する。** ディレクトリごとのサイズと件数を出してから対象を決める。
2. **git 管理下かどうかを確認する**（`git check-ignore -v`）。ignore 対象の派生物は動かしても安全度が高い。
3. **指示の意図（容量を空ける）と手段（data/ を動かす）を分けて考える。**
   意図を達成する手段が別にあるなら、それを提案したうえで実行する。
4. 逆に、**解放量がほぼゼロなのに git 管理下のディレクトリをリンク化するのは、リスクだけ増やす**。やらない判断も報告する。
5. 削除前の検証は「コピーが成功した」ではなく**「コピー先が元と同一である」**を確かめる
   （ファイル数とバイト数の照合 + 実際に使う識別子の抜き取り確認）。

## 28. 「機械規則が負の値を出したら、それは判断を要求しているサイン」

part4 で **3401 帝人**（Base OPM −5.96%）と **7731 ニコン**（−6.83%）が出た。
どちらも §Y / §4-4 のどちらを当てても負で、**ターミナルOPMが負のDCFは企業価値が負になり成立しない**。

減損を除いた実力値を一次ソースで確認したところ、両者は性質がまったく違った:

| | 減損除外後の直近OPM | 判断 |
|---|---|---|
| 3401 帝人 | **+2.09%**（ただし減損が2期連続で売上の9〜10%） | 一過性か構造かが数値から決まらない |
| 7731 ニコン | **−1.96%**（除外してもなお赤字、除外後系列も単調減少） | 減損は原因ではなく症状 |

4307 野村総研（減損を除けば 19.00% で前期超え）と比べると差は明白で、
**「減損があるから一過性」ではない**ことが分かる。

**ルール（自分向け）**
1. **機械規則の出力が定義域の外（負のOPM、負のEV）に出たら、規則を無理に当てはめない。**
   それは「この銘柄はルールで決められない」というサインである。
2. **減損を見つけたら必ず『除いたら黒字か』『除外後の系列も同じトレンドか』を計算する。**
   除いても赤字なら減損は症状であって原因ではない。
3. 一過性かどうかが数値から決まらないものは、**推定でどちらかに倒さずキューへ送る**。
   その際は**除外後の系列を表で残す**（次の人が判断できる材料をそのまま渡す）。

## 29. 既存の Peer が新しい対象になるとき、過去の除外理由がそのまま効いてくる

part4 の15件のうち 7件は、それ以前のウェーブで**Peer として使っていた銘柄**だった。
そして **3659 ネクソンと 7752 リコーは、過去に「D&A が取れない」という理由で Peer から除外していた銘柄**である。

- **3659** は EDINET から D&A・capex を取得できて解決した（前期値が yfinance と完全一致して検証も通った）
- **7752** は EDINET の最新有報が FY2025/3 で、その XBRL にも D&A 要素がなく**解決できずキューへ**

**ルール（自分向け）**
1. **ユニバースを拡張したら、追加銘柄が過去に Peer として除外されていないかを最初に確認する。**
   除外理由は対象になっても消えない。
2. 過去の除外記録（「D&A が取れないので comps に入れない」等）は、
   **将来その銘柄が対象になったときの事前警告として使える**。記録しておく価値がある。
3. 逆に、対象として一次ソースで補完できた銘柄（3659）は、
   **過去に除外した comps CSV に戻せるか**も併せて判断する。

---

## フェーズ2 (2026-09-06) — パイプライン一括修正と全件再生成で得た教訓

### L-P2-1. 先行報告のバグ記述を、再現せずに信じない

`batch_report_20260905.md` §15-6 は「`generate_dcf.py` は `--force` なしで黙って
再生成をスキップし **exit 0** を返す」と記録していた。修正に着手する前に再現を試みたところ、
**`generate_dcf.py` 単体の終了コードは 1 で、validate にも進んでいなかった**。
実体は `batch/regen.sh` がパイプで grep に渡して終了コードを捨てていたことだった。

**規則**: 引き継がれたバグ記述は「症状の観測」として扱い、**原因の記述は自分で再現してから信じる**。
報告どおりに `generate_dcf.py` だけ直していたら、ラッパー側の欠陥は残ったままだった。

### L-P2-2. 「差分ゼロ」は回帰テストの目的ではない

11項目すべてで 5726 の主要46セル回帰を回したが、#6(β)は**意図した差分20件**、
#10(C5)は**4セル**が出るのが正解だった。差分ゼロを目的にすると、正しい修正を
入れられなくなるか、基準を書き換えて誤魔化すことになる。

**規則**: 回帰テストの合格条件は「**意図した差分だけが出ていること**」。
差分が出たら (a) それが意図したものか (b) 意図しない差分がゼロか の2点を毎回明示的に判定し、
基準ワークブックを切り替えたときはその事実を記録する。

### L-P2-3. 完了条件は宣言せず、機械で確かめる

レポートに「中点平均 Target ゼロ」と書く直前に、乖離 > 3.0倍の銘柄の Target 基礎を
機械で数えたところ **4件が中点平均のまま残っていた**。原因は §X と §Y のルール衝突で、
目視では絶対に気づかない類のものだった。

**規則**: 完了条件の各項目に**それを数えるコードを1つずつ**書く。
「〜のはず」でレポートに書かない。この確認1回でルール設計の不整合が1件見つかった。

### L-P2-4. 判定式が「別の現象」でも必ず真になっていないか疑う

§U(トラフ)の判定式は「直近OPM ≤ 窓の p25」。単調減少系列では**直近が定義上つねに最小値**
なので、この式は**あらゆる下降トレンドで必ず発火する**。結果、§Y が「トレンドに平均回帰を
当てるのは統計的な誤り」と明示している、まさにその処理へ銘柄を送っていた。

**規則**: 閾値判定を書くときは「**この式が真になる別の理由**」を必ず列挙する。
トラフの検出式がトレンドでも真になるなら、それはトラフの検出式ではない。

### L-P2-5. 二次ソースの値は、一次ソースで検算してから採用する

yfinance の `beta` フィールドは日本株で NTT −0.165 / 大阪ガス −0.201 / 4205 ちょうど 0.000 /
第一三共・任天堂 null を返す。85件中57件が 0.6 未満だった。
TOPIX に対する2年週次回帰で測り直すと平均 +0.443 → +0.870 になり、
**5726 の overrides に記録されていた手計算 1.553 を 1.547 で再現**した。

**規則**: 外部APIの「指標フィールド」(β・PER 等の計算済み値)は、**自分で計算できるなら計算する**。
採用しない場合も両方を記録し、切り替え可能にしておく(`--source` フラグ)。
リポジトリ内に過去の手計算値があれば、それが最良の検算になる。

### L-P2-6. 同じリポジトリの中に一次ソースがないか先に探す

#7(EDINET 探索窓)と #9(会社予想)は、どちらも `screener/` が既に日次でアーカイブしていた。
`filings` テーブルには有報の docID が 4,208社分そのまま入っており、
`guidance` テーブルには TDnet 決算短信由来の会社予想が 2,984社分入っていた。
**探索窓のヒューリスティクスを直す前に、正解表がすでに手元にあった。**

**規則**: 「取得できない」問題に着手する前に、**同一リポジトリの別サブシステムが
既に持っていないか**を確認する。#7 は API コール 0 回になり、#9 は 0/85 → 78/85 になった。

### L-P2-7. 後処理は再生成で必ず消える

追補6 §X の脚降格は完成したワークブックを編集する後処理であり、`generate_dcf.py` は
その存在を知らない。全件再生成は**17件の裁定をすべて取り消した**
(6857 の Target 4,288 → 8,021 は評価の変化ではなく裁定の不在)。

**規則**: 「生成 → 後処理」の二段構えを採るなら、**再生成のたびに後処理も回す手順**を
運用に組み込む。組み込めないなら生成側に内蔵する。中間状態を人間の記憶に置かない。
